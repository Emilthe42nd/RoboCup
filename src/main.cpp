#include <Arduino.h>
#include "utils.h"

using namespace Robot;

void setup() {
  pinMode(LEDR, OUTPUT);
  pinMode(LEDG, OUTPUT);
  pinMode(LEDB, OUTPUT);
}



void loop() {
  // main loop goes here
}

