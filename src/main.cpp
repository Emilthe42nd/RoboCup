#include <Arduino.h>
#include <utils.cpp>

using namespace Robot;

void setup() {
  pinMode(LEDR, OUTPUT);
  pinMode(LEDG, OUTPUT);
  pinMode(LEDB, OUTPUT);
}



void loop() {
  // main loop goes here
}

